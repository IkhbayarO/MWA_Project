import { LoginManagerDirective } from './login-manager.directive';

describe('LoginManagerDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginManagerDirective();
    expect(directive).toBeTruthy();
  });
});
